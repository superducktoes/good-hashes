const mysql = require('mysql');

// Set database connection credentials
const config = {
    host: 'localhost',
    user: '',
    password: '',
    database: 'hashdb',
};

// create a mysql pool
const pool = mysql.createPool(config);

module.exports = pool;

